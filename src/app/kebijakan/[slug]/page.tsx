const content = { pengiriman: ["Kebijakan pengiriman","Pesanan diproses 1–2 hari kerja. Reguler memerlukan 2–5 hari, Express 1–2 hari, dan pengambilan studio tersedia sesuai jadwal."], retur: ["Kebijakan retur","Retur ukuran dapat diajukan dalam 14 hari setelah pesanan diterima. Produk harus belum dipakai, belum dicuci, dan label masih terpasang."] } as const
export default async function PolicyPage({ params }: { params: Promise<{ slug: keyof typeof content }> }) { const { slug } = await params; const [title,body] = content[slug] ?? content.pengiriman; return <article className="mx-auto min-h-[75svh] max-w-3xl px-5 pb-28 pt-40"><p className="eyebrow text-primary">Informasi pelanggan</p><h1 className="display mt-4 text-6xl sm:text-8xl">{title}</h1><p className="mt-10 text-lg leading-9 text-muted-foreground">{body}</p><h2 className="mt-12 font-serif text-4xl">Hal yang perlu diketahui</h2><ul className="mt-5 flex list-disc flex-col gap-3 pl-5 leading-7 text-muted-foreground"><li>Nomor resi dikirim setelah paket diserahkan ke kurir.</li><li>Hari libur nasional dapat memengaruhi estimasi.</li><li>Hubungi studio dengan nomor pesanan untuk bantuan lebih lanjut.</li></ul></article> }
